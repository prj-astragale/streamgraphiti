# from app.main import app

# import logging
# g_logger = logging.getLogger('g_log')

# in_topic = app.topic("test_topic")
# uentries_topic = app.topic("streamgraphiti-unprocessable-entries")

# @app.agent(in_topic)
# async def reprocessor(stream):
#     async for key, value_greetings in stream.items():
        # If a new mapper compliant with the schema is found, reprocess the message

